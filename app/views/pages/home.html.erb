<%= content_for(:navbar_class, "navbar-home") %>

<div class="banner">
  <div class="banner-content text-center">
    <h1>Hello, this is Match Meal,</h1>
    <p>Let's start cooking!</p>
   <%= link_to "Start now", recipe_path(Recipe.order("RANDOM()").first), class: "btn btn-lg btn-primary" %>
  </div>
</div>
